import 'package:flutter/material.dart';
import 'package:yashcode/homescreen.dart';

void main() {
  runApp(demo());
}

class demo extends StatelessWidget {
  const demo({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home:ass;
    );
  }
}
